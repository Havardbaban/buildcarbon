# BuildCarbon (MVP)

Vite + React + TS + Tailwind.  
Deploy on Vercel: import this repo → Deploy.

## Scripts
- `npm install`
- `npm run dev` (local)
- `npm run build` (production)

## Notes
- Edit `src/App.tsx` to change copy or defaults.
- Currency is EUR; change in `currency()` if needed.
